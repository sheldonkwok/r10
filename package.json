{
  "name": "r10",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter server dev",
    "build": "pnpm --filter client build",
    "test": "pnpm --filter shared test",
    "start": "node --experimental-strip-types packages/server/src/index.ts",
    "check": "pnpm -r exec tsc --noEmit && biome check --write --error-on-warnings . && pnpm test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "typescript": "^5.0.0",
    "vite": "^6.4.1"
  }
}
