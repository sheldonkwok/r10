{
  "name": "r10",
  "private": true,
  "type": "module",
"scripts": {
    "dev": "pnpm --filter server dev",
    "build": "pnpm --filter client build",
    "test": "bun test --filter shared"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
