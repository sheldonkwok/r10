{
  "name": "r10",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:server": "bun run --filter server dev",
    "dev:client": "bun run --filter client dev",
    "test": "bun test --filter shared"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
